apiVersion: v1
kind: Service
metadata:
  name: backend_challenge-app-service
  labels:
    app: backend_challenge-app
spec:
  selector:
    app: backend_challenge-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: NodePort
